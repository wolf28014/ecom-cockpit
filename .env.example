# ===========================================
# 电商经营驾驶舱 Pro - 环境变量示例
# ===========================================
# 复制此文件为 .env 并填写真实值
#
# 本地开发:
#   1. 复制: cp .env.example .env
#   2. 填写 DATABASE_URL（本地 PostgreSQL 或 Vercel Postgres 连接串）
#   3. 运行: bun run db:push && bun scripts/seed.ts
#
# Vercel 部署:
#   1. 在 Vercel 项目设置 → Environment Variables 中添加 DATABASE_URL
#   2. 数据库可使用 Vercel Postgres（免费 256MB）
#      创建地址: https://vercel.com/dashboard/stores
# ===========================================

# PostgreSQL 数据库连接串
# 本地开发示例: postgresql://user:password@localhost:5432/ecom_cockpit
# Vercel Postgres 示例: postgresql://default:xxx@xxx-pooler.xxx.region.postgres.vercel.com/ecom_cockpit
DATABASE_URL="postgresql://user:password@localhost:5432/ecom_cockpit"

# 可选: AI 模型（用于 z-ai CLI 调用 GLM-4）
# 默认使用环境中的 z-ai CLI，无需配置
ZAI_API_KEY=""
